# Associating NSG with Application Subnet

The NSG `nsg-app` was associated with:

- **Subnet:** subnet-app  
- **VNet:** vnet-poradnia  

This ensures that all VMs inside the subnet inherit the same security policies.

